import { HoverStyleDirective } from './hover-style.directive';

describe('HoverStyleDirective', () => {
  it('should create an instance', () => {
    //@ts-ignore
    const directive = new HoverStyleDirective();
    expect(directive).toBeTruthy();
  });
});

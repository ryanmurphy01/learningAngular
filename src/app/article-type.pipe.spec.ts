import { ArticleTypePipe } from './article-type.pipe';

describe('ArticleTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleTypePipe();
    expect(pipe).toBeTruthy();
  });
});
